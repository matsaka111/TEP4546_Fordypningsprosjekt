# Conda environment for TEP4541 project.
# Recreate with:  conda env create -f environment.yml
# Update with:    conda env update -f environment.yml --prune

name: TEP4541
channels:
  - conda-forge

dependencies:
  - python=3.12
  - cantera
  - jupyterlab
  - numpy
  - matplotlib
  - pandas
  - scipy
  - pytorch
